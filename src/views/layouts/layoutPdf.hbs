<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{baseURL}}/css/font-awesome.min.css" />
    <link href="{{baseURL}}/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="{{baseURL}}/css/styles.css">
    <link rel="shortcut icon" href="{{baseURL}}/img/GuardiandelFaro.svg">
    <link rel="stylesheet" href="{{baseURL}}/css/stylesSimple.css">
    <title>FICHA PREVENTIVA PDF</title>
    <style>
        /* ============================
   CONFIGURACIÓN A4
============================ */
        @page {
            size: A4;
            margin: 20mm 15mm 20mm 15mm;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 12px;
            color: #222;
        }

        h1 {
            font-size: 20px;
            margin: 0;
        }

        h2 {
            font-size: 14px;
            margin: 0;
        }

        hr {
            border: none;
            border-top: 2px solid #003366;
            margin: 10px 0 20px 0;
        }

        /* ============================
   CABECERA
============================ */

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .company {
            font-size: 12px;
            text-align: right;
        }

        .title {
            color: #003366;
        }

        /* ============================
   SECCIONES
============================ */

        .section {
            margin-bottom: 5px;
            page-break-inside: avoid;
        }

        .section-title {
            background: #003366;
            color: white;
            padding: 3px 4px;
            font-weight: bold;
            font-size: 12px;
            margin-bottom: 2px;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
        }

        .table th {
            background: #f0f3f7;
            font-weight: bold;
            text-align: left;
            border: 1px solid #ccc;
            padding: 3px;
        }

        .table td {
            border: 1px solid #ccc;
            padding: 3px;
        }

        /* ============================
   ESTADOS
============================ */

        .estado-ok {
            color: #0a7a0a;
            font-weight: bold;
        }

        .estado-ko {
            color: #b30000;
            font-weight: bold;
        }

        .estado-na {
            color: #777;
        }

        /* ============================
   OBSERVACIONES
============================ */

        .observaciones {
            border: 1px solid #ccc;
            padding: 4px;
            margin-top: 5px;
            min-height: 10px;
            white-space: pre-wrap;
        }

        /* ============================
   IMÁGENES
============================ */

        .image {
            margin-top: 3px;
            text-align: center;
        }

        .image img {
            max-width: 100%;
            max-height: 250px;
        }

        /* ============================
   PIE DE PÁGINA
============================ */

        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 10px;
            color: #777;
        }

        .page-break {
            page-break-before: always;
        }

        .SVG-black-logo {
            filter: brightness(0) saturate(100%);
        }
    </style>
</head>

<body>
    {{{body}}}
</body>

</html>